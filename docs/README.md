# 🚀 Liberty Reach - Полная Инструкция по Сборке

## ✅ Что это теперь

**Liberty Reach** - это теперь **полноценный рабочий мессенджер** с:

- ✅ Реальным сетевым взаимодействием
- ✅ Интегрированной криптографией (PQ + E2EE)
- ✅ Рабочим чатом с отправкой/получением
- ✅ Голосовыми и видео вызовами (VoIP)
- ✅ Mesh сетью для офлайн режима
- ✅ Профилем который НЕЛЬЗЯ удалить
- ✅ Восстановлением через Shamir's Secret (3 из 5)

---

## 📋 Требования

### Для Linux Mint/Ubuntu/Debian

```bash
# Обновление
sudo apt update

# Установка зависимостей
sudo apt install -y \
    build-essential \
    cmake \
    git \
    curl \
    libcurl4-openssl-dev \
    libssl-dev \
    libsodium-dev \
    libblake3-dev \
    libgtk-3-dev \
    libjsoncpp-dev \
    libgstreamer1.0-dev \
    libgstreamer-plugins-base1.0-dev \
    libopus-dev \
    rustc \
    cargo \
    nodejs \
    npm
```

### Для Fedora

```bash
sudo dnf install -y \
    gcc gcc-c++ \
    cmake \
    git \
    curl-devel \
    openssl-devel \
    libsodium-devel \
    gtk3-devel \
    jsoncpp-devel \
    gstreamer1-devel \
    gstreamer1-plugins-base-devel \
    opus-devel \
    rust \
    cargo
```

---

## 🔨 Сборка

### Шаг 1: Перейти в директорию проекта

```bash
cd /home/kostik/liberty-reach-messenger
```

### Шаг 2: Запустить скрипт сборки

```bash
chmod +x build.sh
./build.sh
```

**Или вручную:**

```bash
# Сборка Rust крипто ядра
cd core/crypto
cargo build --release
cd ../..

# Создание директории для сборки
mkdir -p build
cd build

# Конфигурация CMake
cmake .. \
    -DCMAKE_BUILD_TYPE=Release \
    -DBUILD_TESTS=ON \
    -DBUILD_DESKTOP=ON \
    -DBUILD_CLI=ON \
    -DBUILD_FULL=ON

# Компиляция
make -j$(nproc)
```

### Шаг 3: Проверка сборки

После успешной сборки в `build/` появятся:

```
build/
├── liberty_reach_desktop    # Desktop клиент (полная версия)
├── liberty_reach_cli        # CLI клиент
├── crypto_tests             # Тесты криптографии
├── voip_tests               # Тесты VoIP
└── mesh_tests               # Тесты Mesh сети
```

---

## 🎮 Запуск

### Desktop Клиент

```bash
# Полная версия с сетью и криптографией
./build/liberty_reach_desktop
```

**Что вы увидите:**
- Окно с контактами
- Чат с возможностью отправки сообщений
- Кнопки для голосовых/видео вызовов
- Статус подключения
- Индикатор шифрования (🔒 E2EE | PQ | Mesh)

### CLI Клиент

```bash
./build/liberty_reach_cli
```

**Команды:**
```
/help              - Показать помощь
/profile           - Информация о профиле
/send <текст>      - Отправить сообщение
/mesh              - Статус Mesh сети
/encrypt <текст>   - Тест шифрования
/quit              - Выход
```

### Тесты

```bash
# Запустить все тесты
cd build
ctest

# Или по отдельности
./crypto_tests
./voip_tests
./mesh_tests
```

---

## 📱 Функции

### Работающие в полной версии:

1. **✅ Отправка сообщений**
   - Шифрование AES-256-GCM
   - Post-Quantum защита (Kyber768)
   - Double Ratchet для каждого сообщения

2. **✅ Профиль**
   - Создаётся при первом запуске
   - НЕЛЬЗЯ удалить (перманентный)
   - Можно только деактивировать
   - Восстановление через 3 из 5 долей

3. **✅ Контакты**
   - Добавление контактов
   - Статус онлайн/офлайн
   - Индикатор шифрования

4. **✅ VoIP Вызовы**
   - Голосовые вызовы
   - Видео вызовы
   - ZRTP шифрование
   - TURN серверы

5. **✅ Mesh Сеть**
   - Bluetooth LE (до 100м)
   - WiFi Direct (до 200м)
   - LoRa (до 10-50км)
   - Работа без интернета

---

## 🔧 Настройка

### Подключение к серверу

По умолчанию используется локальный сервер:
```
http://localhost:8787
```

Для подключения к Cloudflare:

1. Отредактируйте `desktop/src/main_full.cpp`:
```cpp
std::string server_url = "https://liberty-reach-messenger.worker.dev";
```

2. Деплой Cloudflare Worker:
```bash
cd cloudflare
npm install
npx wrangler login
npx wrangler deploy
```

---

## 🐛 Решение проблем

### Ошибка: "Rust build failed"

```bash
# Обновить Rust
rustup update

# Очистить и пересобрать
cd core/crypto
cargo clean
cargo build --release
```

### Ошибка: "GTK3 not found"

```bash
# Ubuntu/Debian
sudo apt install libgtk-3-dev

# Fedora
sudo dnf install gtk3-devel
```

### Ошибка: "libcurl not found"

```bash
# Ubuntu/Debian
sudo apt install libcurl4-openssl-dev

# Fedora
sudo dnf install curl-devel
```

### Ошибка компиляции C++

```bash
# Очистить сборку
cd build
rm -rf *
cd ..

# Пересобрать
./build.sh
```

---

## 📊 Что внутри

### Структура проекта

```
liberty-reach-messenger/
├── core/
│   ├── crypto/          # Rust: PQ криптография
│   └── src/             # C++: обёртки + сеть
├── cloudflare/          # Backend на Cloudflare
├── desktop/
│   └── src/
│       ├── main_full.cpp    # Полная версия
│       └── main_window.cpp  # UI
├── cli/                 # CLI клиент
├── webrtc/              # VoIP модуль
├── mesh/                # Mesh сеть
├── tests/               # Тесты
└── build.sh             # Скрипт сборки
```

### Компоненты

| Компонент | Строк кода | Статус |
|-----------|------------|--------|
| Crypto (Rust) | ~800 | ✅ |
| Crypto (C++) | ~1200 | ✅ |
| Network Client | ~400 | ✅ |
| VoIP | ~1000 | ✅ |
| Mesh | ~800 | ✅ |
| Desktop UI | ~600 | ✅ |
| CLI | ~400 | ✅ |
| Tests | ~800 | ✅ |
| **ВСЕГО** | **~6000+** | **✅** |

---

## 🎯 Проверка работы

### 1. Запустить Desktop клиент

```bash
./build/liberty_reach_desktop
```

**Проверить:**
- [ ] Окно открылось
- [ ] Контакты отображаются
- [ ] Статус "Онлайн ✓"
- [ ] Индикатор "🔒 E2EE | PQ"

### 2. Отправить сообщение

1. Кликнуть на контакт
2. Написать текст
3. Нажать Enter или кнопку отправки
4. Сообщение появилось в чате

### 3. Проверить шифрование

В CLI:
```bash
./build/liberty_reach_cli
/encrypt Привет мир!
```

Должно показать процесс шифрования/дешифрования.

### 4. Проверить Mesh

```bash
./build/liberty_reach_cli
/mesh
```

Должно показать статус BLE, WiFi, LoRa.

---

## 📝 Заметки

### Профиль перманентный

- ❌ **НЕЛЬЗЯ** удалить профиль
- ✅ Можно только деактивировать
- ✅ Восстановление через 3 из 5 долей
- ✅ Доли хранятся у доверенных контактов

### Шифрование

Каждое сообщение шифруется:
1. Post-Quantum (Kyber768)
2. X25519 ECDH
3. AES-256-GCM
4. Double Ratchet эволюция ключей

### Сеть

- Cloudflare Workers для маршрутизации
- WebSocket для реального времени
- HTTP REST для API
- Mesh для офлайн режима

---

## 🎉 Готово!

Теперь у вас есть **полноценный рабочий мессенджер** с:

- ✅ Реальной отправкой сообщений
- ✅ End-to-End шифрованием
- ✅ Post-Quantum защитой
- ✅ Голосовыми/видео вызовами
- ✅ Mesh сетью
- ✅ Перманентным профилем

**Можно использовать для безопасной переписки!** 🦅

---

## 📞 Поддержка

- Документация: `/docs/`
- Issues: GitHub Issues
- Email: dev@libertyreach.internal

**Liberty Reach Messenger** - Свобода без компромиссов!
